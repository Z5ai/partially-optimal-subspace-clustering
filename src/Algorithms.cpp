#include "Algorithms.h"
#include "Graph.h"
#include "Constants.h"

std::vector<edge> Algorithms::evaluate_edge_criterion_cut(Graph graph, auto triple_cost){

};

std::vector<edge> Algorithms::evaluate_edge_criterion_join(Graph graph, auto triple_cost){

};

std::vector<edge> Algorithms::evaluate_triangle_criterion(Graph graph, auto triple_cost){
};
